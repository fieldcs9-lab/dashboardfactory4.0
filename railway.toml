[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPatterns = [
  "index.html",
  "api/**",
  "assets/**",
  "docker/**",
  "Dockerfile"
]

[deploy]
healthcheckPath = "/api/status.php"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
